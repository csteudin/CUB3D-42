#include <stdio.h>
#include <unistd.h>
#include <stdlib.h>



int main(int ac, char **av)
{
    // PSEUDO
    //  PARSING(ac, av)
    //      return(error)
    //  INIT(ac, av)
    // - creates STRUCT
    //      return(error)
    //  RENDER_LOOP(STRUCT)
    //      infinite -> calls clear funciton inside render loop-setup
    
    return (0);
}